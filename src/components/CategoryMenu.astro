---
// src/components/CategoryMenu.astro - GÜNCELLENMIŞ
const categories = [
  { name: 'Pastırma', slug: 'pastirma' },
  { name: 'Sucuk', slug: 'sucuk' },
  { name: 'Mantı', slug: 'manti' },
  { name: 'Kavurma', slug: 'kavurma' },
];

const sarkuteriUrunler = [
  { name: 'Peynir Çeşitlerimiz', slug: 'sarkuteri#peynir' },
  { name: 'Zeytin Çeşitlerimiz', slug: 'sarkuteri#zeytin' },
  { name: 'Bal Çeşitlerimiz', slug: 'sarkuteri#bal' },
  { name: 'Baharat Çeşitlerimiz', slug: 'sarkuteri#baharat' },
];

const digerSayfalar = [
  { name: 'Kayseri Kahvaltısı', slug: 'kayseri-serpme-kahvaltisi' },
  { name: 'Kayseri Sofrası', slug: 'kayseri-sofrasi' },
];
---

<!-- Menü Butonu -->
<button id="category-menu-btn" class="menu-btn" aria-label="Kategoriler Menüsü">
  <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
  </svg>
  <span>Kategoriler</span>
</button>

<!-- Menü Overlay -->
<div id="category-menu-overlay" class="menu-overlay">
  <div id="category-menu" class="category-menu">
    <div class="menu-header">
      <h2>Ürün Kategorileri</h2>
      <button id="close-menu-btn" class="close-btn" aria-label="Kapat">
        <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>

    <nav class="menu-nav">
      <div class="menu-section">
        <h3>Ana Ürünler</h3>
        <ul class="menu-list">
          {categories.map(cat => (
            <li>
              <a href={`/${cat.slug}`} class="menu-item">
                <span>{cat.name}</span>
                <svg width="18" height="18" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div class="menu-section">
        <h3>Şarküteri Ürünleri</h3>
        <ul class="menu-list">
          <li>
            <a href="/sarkuteri" class="menu-item">
              <span>Tüm Şarküteri Ürünleri</span>
              <svg width="18" height="18" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </a>
          </li>
          {sarkuteriUrunler.map(item => (
            <li>
              <a href={`/${item.slug}`} class="menu-item submenu-item">
                <span>{item.name}</span>
                <svg width="18" height="18" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div class="menu-section">
        <h3>Özel Sayfalar</h3>
        <ul class="menu-list">
          {digerSayfalar.map(item => (
            <li>
              <a href={`/${item.slug}`} class="menu-item">
                <span>{item.name}</span>
                <svg width="18" height="18" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div class="menu-section menu-footer">
        <a href="/online-alisveris" class="menu-highlight">
          <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
          </svg>
          <span>Tüm Ürünler</span>
        </a>
      </div>
    </nav>
  </div>
</div>

<style>
  .menu-btn {
    position: fixed;
    top: 20px;
    left: 20px;
    z-index: 999;
    display: flex;
    align-items: center;
    gap: 0.625rem;
    padding: 0.875rem 1.5rem;
    background: white;
    color: #1F2937;
    border: 1px solid #E5E7EB;
    border-radius: 8px;
    font-weight: 600;
    font-size: 0.9375rem;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .menu-btn:hover {
    background: #0891B2;
    color: white;
    border-color: #0891B2;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(8, 145, 178, 0.3);
  }

  .menu-btn svg {
    transition: transform 0.3s ease;
  }

  .menu-btn:hover svg {
    transform: rotate(90deg);
  }

  .menu-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(4px);
    z-index: 1000;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
  }

  .menu-overlay.active {
    opacity: 1;
    visibility: visible;
  }

  .category-menu {
    position: fixed;
    top: 0;
    left: 0;
    bottom: 0;
    width: 100%;
    max-width: 340px;
    background: white;
    transform: translateX(-100%);
    transition: transform 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    overflow-y: auto;
    box-shadow: 4px 0 24px rgba(0, 0, 0, 0.12);
  }

  .menu-overlay.active .category-menu {
    transform: translateX(0);
  }

  .menu-header {
    position: sticky;
    top: 0;
    background: white;
    padding: 1.75rem 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #E5E7EB;
    z-index: 10;
  }

  .menu-header h2 {
    font-size: 1.375rem;
    font-weight: 700;
    margin: 0;
    color: #1F2937;
    letter-spacing: -0.02em;
  }

  .close-btn {
    background: transparent;
    border: none;
    width: 36px;
    height: 36px;
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #6B7280;
    cursor: pointer;
    transition: all 0.3s;
  }

  .close-btn:hover {
    background: #F3F4F6;
    color: #1F2937;
  }

  .menu-nav {
    padding: 1.5rem 1rem;
  }

  .menu-section {
    margin-bottom: 2.5rem;
  }

  .menu-section h3 {
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: #6B7280;
    margin-bottom: 0.75rem;
    padding: 0 0.75rem;
  }

  .menu-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .menu-list li {
    margin-bottom: 0.25rem;
  }

  .menu-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    padding: 0.875rem 0.75rem;
    border-radius: 8px;
    color: #1F2937;
    font-weight: 500;
    font-size: 0.9375rem;
    transition: all 0.25s ease;
    text-decoration: none;
    position: relative;
  }

  .menu-item::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 3px;
    height: 0;
    background: #0891B2;
    border-radius: 0 2px 2px 0;
    transition: height 0.25s ease;
  }

  .menu-item:hover {
    background: #F3F4F6;
    color: #0891B2;
    padding-left: 1rem;
  }

  .menu-item:hover::before {
    height: 60%;
  }

  .menu-item svg {
    flex-shrink: 0;
    opacity: 0.4;
    transition: all 0.25s ease;
  }

  .menu-item:hover svg {
    opacity: 1;
    transform: translateX(2px);
  }

  .submenu-item {
    font-size: 0.875rem;
    padding-left: 1.5rem;
  }

  .menu-highlight {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    padding: 1rem;
    background: #0891B2;
    color: white;
    border-radius: 8px;
    font-weight: 600;
    font-size: 0.9375rem;
    text-decoration: none;
    transition: all 0.3s;
    border: 2px solid #0891B2;
  }

  .menu-highlight:hover {
    background: #0E7490;
    border-color: #0E7490;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(8, 145, 178, 0.3);
  }

  .menu-footer {
    position: sticky;
    bottom: 0;
    background: white;
    padding: 1rem 0 0.5rem;
    border-top: 1px solid #E5E7EB;
    margin-top: auto;
    margin-bottom: 0;
  }

  @media (max-width: 768px) {
    .menu-btn {
      top: 15px;
      left: 15px;
      padding: 0.75rem 1.25rem;
      font-size: 0.875rem;
    }

    .category-menu {
      max-width: 85%;
    }

    .menu-header {
      padding: 1.5rem 1.25rem;
    }

    .menu-header h2 {
      font-size: 1.25rem;
    }

    .menu-nav {
      padding: 1.25rem 0.875rem;
    }
  }

  /* Scrollbar styling */
  .category-menu::-webkit-scrollbar {
    width: 6px;
  }

  .category-menu::-webkit-scrollbar-track {
    background: transparent;
  }

  .category-menu::-webkit-scrollbar-thumb {
    background: #E5E7EB;
    border-radius: 3px;
  }

  .category-menu::-webkit-scrollbar-thumb:hover {
    background: #6B7280;
  }
</style>

<script>
  const menuBtn = document.getElementById('category-menu-btn');
  const menuOverlay = document.getElementById('category-menu-overlay');
  const closeBtn = document.getElementById('close-menu-btn');

  function openMenu() {
    menuOverlay?.classList.add('active');
    document.body.style.overflow = 'hidden';
  }

  function closeMenu() {
    menuOverlay?.classList.remove('active');
    document.body.style.overflow = '';
  }

  menuBtn?.addEventListener('click', openMenu);
  closeBtn?.addEventListener('click', closeMenu);

  // Overlay tıklaması ile kapatma
  menuOverlay?.addEventListener('click', (e) => {
    if (e.target === menuOverlay) {
      closeMenu();
    }
  });

  // ESC tuşu ile kapatma
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && menuOverlay?.classList.contains('active')) {
      closeMenu();
    }
  });

  // Menü linklerine tıklandığında menüyü kapat
  const menuLinks = document.querySelectorAll('.menu-item, .menu-highlight');
  menuLinks.forEach(link => {
    link.addEventListener('click', () => {
      closeMenu();
    });
  });
</script>