---
// src/layouts/BaseLayout.astro
import '../styles/global.css';
import WhatsAppFloat from '../components/WhatsAppFloat.astro';

export interface Props {
  title?: string;
  description?: string;
  image?: string;
  ogType?: string;
}

const {
  title = 'Pastırma Adası - Kayseri Pastırması ve Sucuk',
  description = 'Geleneksel yöntemlerle üretilen kaliteli Kayseri pastırması ve sucuk ürünleri.',
  image = '/og-image.jpg',
  ogType = 'website'
} = Astro.props;

// Canonical URL oluşturma
const siteUrl = Astro.site || new URL(Astro.url.origin);
const canonicalURL = new URL(Astro.url.pathname, siteUrl);
const imageUrl = new URL(image, siteUrl);
---

<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <meta name="generator" content={Astro.generator}>
  <link rel="canonical" href={canonicalURL}>

  <!-- SEO Meta Tags -->
  <title>{title}</title>
  <meta name="description" content={description}>

  <!-- Open Graph / Facebook -->
  <meta property="og:type" content={ogType}>
  <meta property="og:url" content={canonicalURL}>
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:image" content={imageUrl}>

  <!-- Twitter -->
  <meta property="twitter:card" content="summary_large_image">
  <meta property="twitter:url" content={canonicalURL}>
  <meta property="twitter:title" content={title}>
  <meta property="twitter:description" content={description}>
  <meta property="twitter:image" content={imageUrl}>
</head>
<body>
  <slot name="header" />

  <main>
    <slot />
  </main>

  <slot name="footer" />

  <!-- WhatsApp Float Button -->
  <WhatsAppFloat />
</body>
</html>

<style is:global>
  /* BaseLayout'a özel reset ve temel stiller */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    line-height: 1.6;
    color: #1F2937;
    background: #fff;
    overflow-x: hidden;
  }

  main {
    min-height: 60vh;
  }

  img {
    max-width: 100%;
    height: auto;
    display: block;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  button {
    font-family: inherit;
  }

  /* Container - Global utility */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  /* Accessibility */
  :focus-visible {
    outline: 2px solid #0891B2;
    outline-offset: 2px;
  }

  /* Smooth scrolling için Safari fix */
  @media (prefers-reduced-motion: reduce) {
    html {
      scroll-behavior: auto;
    }
  }

  /* Utility Classes */
  .text-center {
    text-align: center;
  }

  .fade-in {
    animation: fadeIn 0.3s ease;
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
</style>